services:
  pico-lobby:
    build:
      context: .
      dockerfile: docker/Dockerfile
      args:
        BINARY_NAME: pico_lobby
    image: ghcr.io/quozul/picolimbo:latest
    ports:
      - "30066:25565"
